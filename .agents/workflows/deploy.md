# Deployment Workflow

1. Run `npm run build` locally to verify there are no compilation errors.
2. Ensure all assets are properly linked and optimized.
3. Push changes to the `main` branch.
4. If using Vercel or Netlify, the integration will automatically trigger a build and deployment.
5. Perform a quick smoke test on the live production URL.
